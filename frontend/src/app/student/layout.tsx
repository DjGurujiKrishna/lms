/** Student area — pages wrap themselves in StudentShell with titles (steps 16 & 19). */
export default function StudentRootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
