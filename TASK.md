1. ✅ similar to user we also need to memoize the /dashboard. which also loading each tme we go out and come.

2. ✅ chart/graph is now showing. bar/line chart based on user selection.
3. ✅ added extra charts as separate cards (users by role + totals).